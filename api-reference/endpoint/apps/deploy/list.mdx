---
title: "List Deployments"
sidebarTitle: "[...]/deployments"
description: "Get the last 10 deployments of an application from the last 24 hours."
api: "GET https:/api.dscloud.pt/v1/apps/{app_id}/deployments"
authMethod: "none"
---

<ParamField header="Authorization" type="string" placeholder="API KEY" required>
  The API key for your account. You can find this in your [account settings](https://dscloud.pt/dashboard/settings).
</ParamField>

<ParamField path="app_id" type="string" placeholder="Application ID" required>
  The ID of the application. You can find this in the URL of your application's dashboard.
</ParamField>

### Response

<ResponseField name="status" type="string">
  Indicates whether the call was successful. `success` if successful, `error` if not.
</ResponseField>

<ResponseField name="response" type="array">

The contents of the response.

<Expandable title="Toggle object">

<ResponseField name="id" type="string">
  The ID of the deploy.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the deploy. Can be `pending`, `clone`, `success`, or `error`.
</ResponseField>

<ResponseField name="date" type="string">
  The date the deploy was created.
</ResponseField>

</Expandable>
</ResponseField>


<ResponseExample>
```json
{
  "status": "success",
  "response": [
    [
      {
        "id": "git-7d3b1a9f8c2e5b24f15e47c8",
        "state": "pending",
        "date": "2024-12-03T10:00:00.000Z"
      },
      {
        "id": "git-7d3b1a9f8c2e5b24f15e47c8",
        "state": "clone",
        "date": "2024-12-05T12:30:10.000Z"
      },
      {
        "id": "git-7d3b1a9f8c2e5b24f15e47c8",
        "state": "success",
        "date": "2024-12-08T14:45:20.000Z"
      }
    ]
  ]
}
```
</ResponseExample>