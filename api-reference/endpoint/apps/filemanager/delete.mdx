---
title: "Delete Application File"
sidebarTitle: "/v1/apps/:appId/files"
description: "Delete a file from your application."
api: "DELETE https://api.dscloud.com/v1/apps/{app_id}/files"
authMethod: "none"
---

<ParamField header="Authorization" type="string" placeholder="API KEY" required>
  The API key for your account. You can find this in your [account settings](https://dscloud.com/dashboard/settings).
</ParamField>

<ParamField path="app_id" type="string" placeholder="Application ID" required>
  The ID of the application. You can find this in the URL of your application's dashboard.
</ParamField>

<ParamField body="path" type="string" placeholder="Path to file" required>
  The path of the file to delete.
</ParamField>

### Response

<ResponseField name="status" type="string">
  Indicates whether the call was successful. `success` if successful, `error` if not.
</ResponseField>

<ResponseExample>
```json
{
    "status": "success"
}
```
</ResponseExample>